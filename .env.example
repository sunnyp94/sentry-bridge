# Alpaca — get keys from https://alpaca.markets (Paper for testing, Live for real)
APCA_API_KEY_ID=your_key_id
APCA_API_SECRET_KEY=your_secret_key

# Data feed: sip (default) or iex (free tier)
ALPACA_DATA_FEED=sip

# Universe and active symbols (required for scanner/discovery)
SCREENER_UNIVERSE=r2000_sp500_nasdaq100
ACTIVE_SYMBOLS_FILE=data/active_symbols.txt

# Paper trading (place orders on Alpaca paper account)
TRADE_PAPER=true

# Discovery: how many top symbols to hand off to the engine (default 10)
DISCOVERY_TOP_N=10

# -----------------------------------------------------------------------------
# Live trading (only when ready): use Live API keys, then set:
#   TRADE_PAPER=false
#   LIVE_TRADING_ENABLED=true
#   APCA_API_BASE_URL=https://api.alpaca.markets
# (Paper: APCA_API_BASE_URL can be omitted; Go defaults to paper-api.alpaca.markets.)
# -----------------------------------------------------------------------------
# Smart Position Management: EOD prune at 15:50 ET — close only losers (not blanket close)
# EOD_PRUNE_AT_ET=15:50
# EOD_PRUNE_STOP_LOSS_PCT=-2.0
# -----------------------------------------------------------------------------
